[package]
name = "cmd-proof"
description = "Tamper-evident evidence for one execution: intent, plan, approval, action, post-state, verdict"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cmd-types = { path = "../cmd-types" }
cmd-approval = { path = "../cmd-approval" }
serde.workspace = true
serde_json.workspace = true
sha2 = "0.10"

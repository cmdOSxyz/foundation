[package]
name = "cmd-policy"
description = "Alios supervision: R0-R3 gate, mandate verification, budget enforcement"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cmd-types = { path = "../cmd-types" }
